<nav id="navigation">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="block">
                    <nav class="navbar navbar-default">
                      <div class="container-fluid">
                        <!-- Brand and toggle get grouped for better mobile display -->
                        <div class="navbar-header">
                          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                          </button>
                              <%= link_to(root_url, class: "logo") do %>
                                <%= image_tag "list2.png" %>
                              <% end %>
                              <div>
                                <%= link_to url_for( :locale => 'en' ) do %>
                                  <%= image_tag "usa.png" %>
                                <% end %>
                                <%= link_to url_for( :locale => 'pt' ) do %>
                                  <%= image_tag "brazil.png" %>
                                <% end %>
                              </div>
                        </div>

                        <!-- Collect the nav links, forms, and other content for toggling -->
                        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                          <ul class="nav navbar-nav navbar-right" id="top-nav">
                            <li> <%= link_to t('menus.home'), root_url %> </li>
                            <li> <%= link_to t('menus.lists'), lists_url %> </li>
                            <li> <%= link_to t('menus.users'), users_url %> </li>
                            <li> <%= link_to t('menus.contact'), contact_url %> </li>
                            
                            <% if !logged_in? %>
                              <li>  <%= link_to "Login/Cadastro", login_url, class: "account" %> </li>
                            <% else %>
                              <li class="dropdown gravatar">
                        				<a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        					<span class="username"> <%= gravatar_for %> </span>
                        					<b class="caret"></b>
                        				</a>
                        				<ul class="dropdown-menu extended logout">
                        					<div class="log-arrow-up"></div>
                        					<li class="eborder-top">
                      							<%= link_to current_user, method: :get do %>
                        							<i class="icon_key_alt"></i> <%= t('actions.profile') %></a>
                        						<% end %>
                        					</li>
                        					<li class="eborder-top">
                        						<%= link_to user_lists_path(current_user), method: :get do %>
                        							<i class="icon_key_alt"></i> <%= t('menus.my_lists') %></a>
                        						<% end %>
                        					</li>
                        					<li>
                        						<%= link_to logout_url, method: :delete, id: 'logout' do %>
                        							<i class="icon_key_alt"></i> <%= t('actions.logout') %></a>
                        						<% end %>
                        					</li>
                        				</ul>
                        			</li>
                            <% end %>
                          </ul>
                        </div><!-- /.navbar-collapse -->
                      </div><!-- /.container-fluid -->
                    </nav>
                </div>
            </div><!-- .col-md-12 close -->
        </div><!-- .row close -->
    </div><!-- .container close -->
</nav><!-- header close -->